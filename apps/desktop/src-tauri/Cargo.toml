[package]
name = "lumen-desktop"
version = "0.1.0"
description = "Lumen desktop application"
authors = ["Lumen Lab"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
